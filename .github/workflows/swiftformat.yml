name: swiftformat
on:
  pull_request:
    paths:
      - '.github/workflows/swiftformat.yml'
      - '**/*.swift'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  swiftformat:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nicklockwood/swiftformat:0.55.0
    steps:
    - uses: actions/checkout@v4
    - name: swiftformat
      run: swiftformat . --lint --lenient
